{
  "name": "mlr-server",
  "version": "1.0.0",
  "description": "multiple linear regression with graph",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "alex",
  "license": "ISC",
  "dependencies": {
    "canvas": "^2.10.1"
  }
}
